apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-app-prod
  namespace: prod
spec:
  replicas: 4
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: nginx-prod-app
  template:
      metadata:
        labels:
          app: nginx-prod-app
      spec:
        containers:
        - name: nginx
          image: nginx:1.27
          ports:
          - containerPort: 80
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"  
            limits:
              cpu: "250m"
              memory: "256Mi"
