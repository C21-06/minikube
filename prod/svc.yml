apiVersion: v1
kind: Service
metadata:
  name: nginx-prod-service
spec:
  type: NodePort
  selector:
    app: nginx-prod-app         #name from deployments.yml
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30082

