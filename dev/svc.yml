apiVersion: v1
kind: Service
metadata:
  name: nginx-dev-service
  namespace: dev
spec:
  type: NodePort
  selector:
    app: nginx-dev-app         #name from deployments.yml
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30081

